export enum ColumnId {
    TODO = 'todo',
    IN_PROGRESS = 'in-progress',
    DONE = 'done',
}

export const ColumnIdValues = Object.freeze([
    ColumnId.TODO,
    ColumnId.IN_PROGRESS,
    ColumnId.DONE,
]);
