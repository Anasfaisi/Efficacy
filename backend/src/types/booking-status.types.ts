export enum BookingStatus {
    PENDING = 'pending',
    CONFIRMED = 'confirmed',
    RESCHEDULED = 'rescheduled',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    MISSED_BY_MENTOR = 'missed_by_mentor',
    MISSED_BY_STUDENT = 'missed_by_student',
}
